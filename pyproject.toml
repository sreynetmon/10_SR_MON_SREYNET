[project]
name = "chat-with-document-app"
version = "0.1.0"
description = ""
authors = [
    {name = "shuni"}
]
requires-python = ">=3.14"
dependencies = [
    "chromadb (>=1.5.9,<2.0.0)",
    "ollama (>=0.6.2,<0.7.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
